language: node_js
node_js:
  - "7.10"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
before_deploy:
  - mkdir gh-pages
  - cp index.html gh-pages
  - webpack ./index.js gh-pages/bundle.js
  - cd gh-pages
